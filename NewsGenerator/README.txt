This sample code uses BeautifulSoup to scrape through HTML of a few news sites, and fetches headlines and saves them to a txt.

*At the moment, this works for CNN, but I find that for BBC the HTML formating changes frequently, meaning it's buggy at the moment. Might play with it later.